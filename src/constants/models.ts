export interface MyTheme {
  background: string;
  color: string;
  profilePhoto: string;
  profilePhotoAlign: string;
  bgImage: string;
  bgImageWide: string;
  idle: string;
  accent: string;
}

export interface Slide {
  slideName: string;
  link: string;
  techLogos: string[];
  imageSrc: string;
}
